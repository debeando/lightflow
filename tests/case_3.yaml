---
general:
  tmp_dir: /tmp
tasks:
  - name: T1
    loops:
      - name: T1L1
      - name: T1L2
    pipes:
      - name: T1P1
        command: echo -e "{{ .date }}"
      - name: T1P2
        command: echo -e "{{ .date }}"
  - name: T2
    pipes:
      - name: T2P1
        command: echo -e "{{ .date }}"
      - name: T2P2
        command: echo -e "{{ .abc }}"
      - name: T2P3
        command: exit 123
