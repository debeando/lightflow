---
general:
  tmp_dir: /tmp
tasks:
  - name: demo
    subtask:
      - name: A1
        variables:
          case_loop_var: "DEMO ({{ .date }})"
    pipes:
      - name: print
        execute: echo -e "({{ .date }}) {{ .case_pipe_var }}"
        variables:
          case_pipe_var: "TEST {{.case_loop_var}}"
