---
tasks:
  - name: T1
    subtask:
      - name: T1ST1
    pipes:
    - name: T1ST1P1
      execute: echo -n "{{ .date }}"
  - name: T2
    subtask:
      - name: T2ST1
    pipes:
    - name: T2ST1P1
      execute: echo -n "{{ .date }}"
