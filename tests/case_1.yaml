---
tasks:
  - name: curl
    loops:
      - name: basic
    pipes:
    - name: curl
      command: curl --silent ifconfig.me/all.json
      format: JSON
      Retry:
        Attempts: 1
        Wait: 1
