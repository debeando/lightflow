---
tasks:
  - name: curl
    loops:
      - name: basic
    pipes:
    - name: curl
      execute: curl --silent ifconfig.me/all.json
      format: JSON
      Retry:
        Attempts: 1
        Wait: 1
    - name: ip_addr
      execute: curl --silent ifconfig.me/all.json | jq '.ip_addr' -r
