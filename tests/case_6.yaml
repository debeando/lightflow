---
tasks:
  - name: check
    loops:
      - name: main
        variables:
          chunk.limit: 2
    pipes:
    - name: total
      execute: echo -n "9"
      register: chunk.total
    - name: import
      execute: echo -e "SELECT * FROM test LIMIT {{ .limit }} OFFSET {{ .offset }};"
      # execute: echo -e "SELECT * FROM test LIMIT {{ .limit }} OFFSET {{ .offset }};"
